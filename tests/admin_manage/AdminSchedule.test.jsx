import React from 'react';
import ReactTestUtils from 'react-dom/test-utils';
import AdminSchedules from '../../app/components/admin_manage/AdminSchedules';

describe('testing AdminSchedules', () => {
  // const newAdminSchedules = ReactTestUtils.renderIntoDocument(<AdminSchedules />); // eslint-disable-line
  // const inputs = ReactTestUtils.scryRenderedDOMComponentsWithTag(newMeetingDayBlock, 'input');
  // const onSubmitFn = jest.fn();

  beforeEach(() => {
  });

  test('form submits with valid data', () => {
    expect(true).toBe(true);
  });
});
